# Mantiene el paquete publicado al ComfyUI Registry reducido al runtime del nodo.
# Sintaxis = .gitignore (capa sobre .gitignore; lo no-trackeado ya queda fuera).
# Excluye archivos de desarrollo/CI que no se ejecutan en runtime.
tests/
.github/
